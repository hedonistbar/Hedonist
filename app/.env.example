# All optional — the app falls back to the Hedonist pilot project's public
# values (safe to expose; every table is protected by RLS). Override only if
# you're pointing the app at a different Supabase project or restaurant.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_DEFAULT_RESTAURANT_SLUG=
