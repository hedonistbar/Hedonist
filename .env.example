# Telegram
TELEGRAM_BOT_TOKEN=

# Supabase / Postgres
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# Direct Postgres connection string, used only by `npm run migrate` (DDL needs a raw SQL connection).
# Supabase dashboard -> Settings -> Database -> Connection string (URI).
SUPABASE_DB_URL=

# Claude API (used from Phase 1 onward)
ANTHROPIC_API_KEY=

# Default restaurant for single-tenant local runs (Hedonist pilot)
DEFAULT_RESTAURANT_SLUG=hedonist
