# Taskboard is its own Supabase project — separate from Hedonist's. Create a
# free project at https://supabase.com, apply migrations/*.sql (SQL Editor,
# in order), then fill these in from Project Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
